---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: planets.astronomy.galaxy.kubesphere.io
spec:
  group: astronomy.galaxy.kubesphere.io
  names:
    kind: Planet
    listKind: PlanetList
    plural: planets
    shortNames:
    - planet
    singular: planet
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: |-
          Planet is the Schema for the planets API
          Label: kubesphere.io/galaxy - references the GalaxySpec
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: PlanetSpec defines the desired state of Planet
            properties:
              diameterKm:
                description: Diameter in kilometers
                type: number
              hasLife:
                description: Does the planet support life?
                type: boolean
              moons:
                description: List of moons
                items:
                  type: string
                type: array
              name:
                description: Name of the planet
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources: {}
